// CommonModel class represents a basic model with image and title properties
class CommonModel {
  constructor(image, title) {
    this.image = image;
    this.title = title;
  }
}

export default CommonModel;
